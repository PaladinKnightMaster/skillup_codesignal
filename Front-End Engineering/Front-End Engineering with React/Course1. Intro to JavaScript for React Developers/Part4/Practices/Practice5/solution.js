```
// TODO: Change the import to default import format with the name rotateFunction
import { rotate } from './cosmosActions.js';

// Don't forget to update the function call
const message = rotate();

document.getElementById('exportStatus').textContent = message;
```

// TODO: Change the import to default import format with the name rotateFunction
import { rotateFunction } from './cosmosActions.js';

// Don't forget to update the function call
const message = rotateFunction();

document.getElementById('exportStatus').textContent = message;