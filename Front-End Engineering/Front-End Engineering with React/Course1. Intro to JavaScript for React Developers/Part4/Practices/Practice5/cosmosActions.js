```
// TODO: Change the export to match the default export syntax.
export function rotate() {
    return 'The cosmos rotates!';
}
```
// TODO: Change the export to match the default export syntax.
export function rotateFunction() {
    return 'The cosmos rotates!';
}