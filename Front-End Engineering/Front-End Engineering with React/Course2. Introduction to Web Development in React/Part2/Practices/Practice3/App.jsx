/*
const Spacecraft = () => {
  return <button onClick={() => alert('Welcome on board!')}>Launch;
}

export default Spacecraft;
*/

const Spacecraft = () => {
  return <button onClick={() => alert('Welcome on board!')}>Launch</button>;
}

export default Spacecraft;