/*
fn main() {
    // Define a variable of type 'i32' representing days until the event completion
    let time_to_event: i32 = 10;
    println!("There are {} days to the event completion.", time_to_event);
}
*/

fn main() {
    // Define a variable of type 'i32' representing days until the event completion
    let time_to_event: f32 = 10.5;
    println!("There are {} days to the event completion.", time_to_event);
}