/*
fn main() {
    let earth_orbit_radius = 149600000; // in kilometers
    println!("The Earth's orbit radius is {} kilometers.", earth_orbit_radius);

    let 8_planets = 8; // Incorrect naming
    println!("There are {} planets in the Solar System.", 8_planets);
}
*/

fn main() {
    let earth_orbit_radius_km = 149600000; // in kilometers
    println!("The Earth's orbit radius is {} kilometers.", earth_orbit_radius_km);

    let planets_count = 8; // Incorrect naming
    println!("There are {} planets in the Solar System.", planets_count);
}